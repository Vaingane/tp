<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Bienvenue !</title>
</head>
<body>
<h1>Bienvenue {{ prenom }}</h1>
<p>
    <table>
        Mes jeux préférés sont&nbsp;:
        {% set i = 1 %}
        {% for jeu in jeux %}
            <tr>
                <td>{{ i }}</td><td>{{ jeu }}</td>             {# i peut être avantageusement remplacé par loop.index #}
            </tr>
                {% set i = i+1 %}
        {% else %}{# utilisé si aucune itération n'est effectuée #}
            <tr>
                <td>je ne préfère aucun jeu&nbsp;!</td>
            </tr>
        {% endfor %}
    </table>
</p>
</body>
</html>
